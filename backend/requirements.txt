# OpenTap Backend Dependencies
# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
geoalchemy2==0.15.2
alembic==1.14.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (for OSM import, Twilio, etc.)
httpx==0.28.1

# SMS (optional, enable when ready)
# twilio==9.4.0

# Image processing (for photo thumbnails)
Pillow==11.1.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# Dev
python-dotenv==1.0.1
